---
title: DigitalOcean Spaces
description: Configure the digitaloceanSpaces adapter for DigitalOcean Spaces.
---

Use `digitaloceanSpaces` for DigitalOcean Spaces buckets.

## Basic Setup

```ts
import { digitaloceanSpaces } from "vs3/adapters";

const adapter = digitaloceanSpaces({
  region: "nyc3",
  credentials: {
    accessKeyId: process.env.DO_SPACES_KEY!,
    secretAccessKey: process.env.DO_SPACES_SECRET!,
  },
});
```

## Options

```ts
{
  region: string; // e.g. "nyc3", "ams3", "sgp1"
  credentials: {
    accessKeyId: string;
    secretAccessKey: string;
  };
}
```

## Notes

- Endpoint is generated as `https://<region>.digitaloceanspaces.com`.
- The preset uses virtual-host style addressing (`forcePathStyle: false`).

## Next Steps

- [Overview](/docs/adapters/overview) for adapter model details.
- [Custom Adapter](/docs/adapters/custom-adapter) if you need custom S3 client options.
