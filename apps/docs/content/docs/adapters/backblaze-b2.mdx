---
title: Backblaze B2
description: Configure the backblazeB2 adapter for Backblaze S3-compatible storage.
---

Use `backblazeB2` for Backblaze B2 S3-compatible buckets.

## Basic Setup

```ts
import { backblazeB2 } from "vs3/adapters";

const adapter = backblazeB2({
  region: "us-west-000",
  credentials: {
    accessKeyId: process.env.B2_ACCESS_KEY_ID!,
    secretAccessKey: process.env.B2_SECRET_ACCESS_KEY!,
  },
});
```

## Options

```ts
{
  region: string; // e.g. "us-west-000", "eu-central-003"
  credentials: {
    accessKeyId: string;
    secretAccessKey: string;
  };
}
```

## Notes

- Endpoint is generated as `https://s3.<region>.backblazeb2.com`.
- Use the exact B2 region string from your bucket configuration.

## Next Steps

- [Overview](/docs/adapters/overview) for adapter model details.
- [Custom Adapter](/docs/adapters/custom-adapter) for provider-specific overrides.
