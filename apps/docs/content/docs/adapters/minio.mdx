---
title: MinIO
description: Configure the minio adapter for MinIO deployments.
---

Use `minio` for self-hosted or managed MinIO instances.

## Basic Setup

```ts
import { minio } from "vs3/adapters";

const adapter = minio({
  endpoint: "http://localhost:9000",
  credentials: {
    accessKeyId: process.env.MINIO_ACCESS_KEY!,
    secretAccessKey: process.env.MINIO_SECRET_KEY!,
  },
});
```

## Options

```ts
{
  endpoint: string;
  credentials: {
    accessKeyId: string;
    secretAccessKey: string;
  };
  region?: string; // default: "us-east-1"
}
```

## Notes

- This preset enables `forcePathStyle: true` automatically, which MinIO typically requires.
- For local development, keep endpoint/protocol aligned with your MinIO server config.

## Next Steps

- [Overview](/docs/adapters/overview) for adapter model details.
- [Custom Adapter](/docs/adapters/custom-adapter) if you need additional client-level tuning.
