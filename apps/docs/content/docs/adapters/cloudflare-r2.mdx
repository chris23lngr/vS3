---
title: Cloudflare R2
description: Configure the cloudflareR2 adapter for Cloudflare R2 buckets.
---

Use `cloudflareR2` for Cloudflare R2 storage.

## Basic Setup

```ts
import { cloudflareR2 } from "vs3/adapters";

const adapter = cloudflareR2({
  accountId: process.env.CF_ACCOUNT_ID!,
  credentials: {
    accessKeyId: process.env.R2_ACCESS_KEY_ID!,
    secretAccessKey: process.env.R2_SECRET_ACCESS_KEY!,
  },
});
```

## Options

```ts
{
  accountId: string;
  credentials: {
    accessKeyId: string;
    secretAccessKey: string;
  };
  jurisdiction?: "eu" | "fedramp";
}
```

## Notes

- The adapter sets `region: "auto"` and builds the endpoint from `accountId`.
- Set `jurisdiction` only if your R2 account uses data locality constraints.

## Next Steps

- [Overview](/docs/adapters/overview) for adapter model details.
- [Custom Adapter](/docs/adapters/custom-adapter) for non-preset S3-compatible setups.
